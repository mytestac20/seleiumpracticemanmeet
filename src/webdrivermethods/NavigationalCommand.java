package webdrivermethods;

public class NavigationalCommand {

    //navigate().to()
    //navigate().back()
    //navigate().forward()
    //navigate().refresh()

    //driver.get() accept URL in string format only
    //driver.nvigate().to() accepts URL in the string format & URL object format

    //getWindowHandle()
    //getWindowHandles()
    //driver.switchTo().window(windowID)
    //session28


}
